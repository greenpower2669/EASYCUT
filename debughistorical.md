# debughistorical.md — FabVidEdit

## FABVID-EXP-001 — paramètres export perdus (cause confirmée par lecture du code)
Symptôme rapporté par Fab : formats de sortie et paramètres revenus incorrects. Dans la v0.25 le moteur déclare `ExportVideoCodec`, `ExportFrameRate`, `ExportResolution`, `ExportBitrate`, mais `EditorScreen.ExportOptionsDialog` n'offre que 720/1080/4K et envoie un `Int`. Le ViewModel crée alors un `ExportSettings` par défaut, alors que le dialogue affiche H.264 et le défaut moteur était H.265. Correction sur cette branche : paramètre complet UI → moteur, H.264 par défaut, H.265 exposé avec garde matérielle. Pas encore validé sur téléphone. Intervalle continu 2–50 non encore réalisé.

## FABVID-PRV-001 — aperçu défaillant (symptôme, cause exacte inconnue)
Fab signale la prévisualisation buguée. Risques observés dans le code : lecteur recréant la composition après `project.updatedAt` ; `remember(transform)` pouvant annuler un geste en cours ; rendu en pleine résolution coûteux, multi-pistes Media3 à vérifier. Améliorations partielles : préserver l'état gestuel et plafonner l'aperçu. Ne pas déclarer le bug corrigé sans reproduction, logs et comparaison visuelle preview/export sur téléphone.

## FABVID-LRG-001 — gros médias non optimisés (symptôme et mécanismes confirmés)
L'import clone la source puis matérialise les streams ; pour les médias déjà détenus par l'app, une copie supplémentaire était évitable. Le proxy TS MP4 de travail demandait `+faststart` malgré sa nature locale/temporaire. Correction ciblée : réutilisation des sources app-owned, suppression du second passage faststart, vérification capacité avant le remux. Sources externes toujours clonées par sécurité. Mesures de latence, pic de stockage, mémoire GPU et extraction en cas de manque d'espace restent à effectuer.

## FABVID-CI-001 — premier build bloqué sur un test obsolète
Le workflow du commit `e8ddcec6e5d51951d8ff609d29dacc9e227dac6d` a exécuté 29 tests, dont un échec : `ExportFormatOptionsTest` attendait encore source/24/25/30/60 après l'ajout du réglage 50 i/s dans `ExportFrameRate`. Le CI s'est arrêté avant `assembleDebug` et `bundleDebug` ; aucun APK/AAB généré par ce run. Correction : assertion et nom du test alignés sur le contrat source/24/25/30/50/60. Nouveau résultat CI à vérifier ; ne pas déclarer la compilation réussie sur la base de ce correctif seul.

## FABVID-CI-002 — compilation OK, transfert d’artefacts bloqué
Le CI du commit `98992bcabe5c046703585cc43c1c2d4dd0d9be19` a terminé Gradle avec `BUILD SUCCESSFUL`, produit et renommé APK et AAB, mais les deux étapes `actions/upload-artifact@v4` ont échoué : `Artifact storage quota has been hit`. Ce n'est ni une erreur Kotlin ni un échec de compilation. Correctif de livraison : publier en GitHub prerelease via `gh release create` avec `GITHUB_TOKEN` et sans upload-artifact. Succès de la prerelease encore à vérifier ; ne pas inventer de liens de téléchargement.

## FABVID-LRG-002 — import vidéo de 1 Go, plantage Android (symptôme rapporté, cause non confirmée sans logcat)
Fab a observé « Nettoyage et analyse des streams… », suivi d'un arrêt systématique sur un fichier de 1 Go. Cause *plausible étayée par code* : `ResolutionFrameScanner` demandait `-show_frames` sur toute la vidéo, récupérait la sortie entière dans `session.output`, puis créait des listes complètes et tris de toutes les images ; mémoire croissante selon durée/cadence. L'appel de `cleanupOrphans` avant import était synchrone sur le fil UI : source possible de gel. Correction ciblée v0.25.2 : sortie FFprobe native `-o` vers métadonnées locales temporaires, images-clés et lecture par lignes sans collection globale ; traitements fichiers sur IO ; statut d'étape ; `finally` UI ; protection du média déjà détenu par l'app ; suppression du fichier temporaire. Le crash exact (Java OOM, FFmpeg natif, stockage) et l'efficacité sur 1 Go restent non vérifiés : exiger test téléphone et journal si échec. Le mode images-clés peut manquer un redimensionnement sans keyframe, non déclaré équivalent à un scan complet.

## FABVID-ERR-003 — v0.25.2 se ferme sans message à chaque essai (rapport Fab)
Démonstration du 22 septembre : le 1 Go entraîne encore une fermeture sans alerte après v0.25.2, malgré le scanner ligne-par-ligne. Il est incorrect de déclarer le crash réglé par la seule baisse de mémoire Java. Lecture du code : `MainActivity` lançait un `cleanupStartupOrphans` en parallèle et `FabVidEditViewModel.createProject/addClips` faisaient un `cleanupOrphans` destructeur ; la réclamation supprime tout fichier non encore présent dans `_projects` et purge le cache FFmpeg, sans distinguer clone/extraction actifs. COURSE DE FICHIERS POSSIBLE, pas preuve du crash. Autres hypothèses : mémoire native FFmpeg/HEVC, Android LMK, exception JVM, manque disque ; logcat/rapport nécessaire.
Correction préventive : disable sweeps auto jusqu'à leases/transaction ; journal local borné, stade + stack Java, récupération motif `ApplicationExitInfo` au relancement, UI GET ERR ; scan grands fichiers en 16 fenêtres d'images clés de 2s pour borner le travail de décodage natif. Les résultats pour changements de résolution rares deviennent approximatifs ou manqués ; suivre ce compromis, ne pas déclarer conforme sans vidéo réelle. Le crash SIGSEGV peut tuer le processus avant l'écriture Java : consulter sortie Android, et si les journaux sont insuffisants, collecter logcat externe ciblé.

## FABVID-PRV-004 — cause exacte du crash du 1 Go établie par GET ERR v0.25.3
Rapport fourni par Fab : STAGE IMPORT_START → copie → inventaire → scan → vidéo 1/1 → audio 1/1 → OPEN_PROJECT clips=1 → UNCAUGHT main. Exception `java.lang.UnsupportedOperationException: class android.view.TextureView. Use CompositionPlayer.setVideoSurface() for Surface output.`, dans `androidx.media3.transformer.CompositionPlayer.handleSetVideoOutput` via `SimpleBasePlayer.setVideoTextureView` → `PlayerView.setPlayer` → `CapCutMultitrackEditorV06.kt:489`. Android confirme `reason=Crash Java/Kotlin`. Cause DIRECTE confirmée : sortie vidéo du lecteur multipiste branchée via l'API PlayerView/TextureView que CompositionPlayer n'accepte pas. Ce n'est pas un OOM de scan et cette exécution a importé le média de 1 Go correctement ; les risques d'import et disque restent distincts. Correctif 0.25.4 : Surface explicite, lifecycle/clear, fallback ExoPlayer si composition ne prend pas la surface, et l'autre écran `EditorScreen` corrigé pour la même faute. Tester réellement sur appareil : Media3 CompositionPlayer 1.11 peut présenter d'autres limites de surface ou décodage ; n'affirmer ni preview ni crash résolu sans essais.

## FABVID-CI-004 — premier build surface v0.25.4
Le CI du commit `7276f5ba5d7025ce888bbcc88ec3cd766878aea5` échoue en compilation Kotlin : `CapCutMultitrackEditorV06.kt:487,505 Unresolved reference 'aspectRatio'` ; la version Media3 utilisée exige `AspectRatioFrameLayout.setAspectRatio(outputRatio)` et non la propriété Kotlin assignée. Correction dans un commit intégrant les quatre fichiers FAB. Aucun APK v0.25.4 depuis le premier build.

## FABVID-GEST-005 — décalage du drag après zoom et pivot involontaire (observation Fab, 22/09/2026)
Symptôme rapporté sur la v0.25.4 : zoom OK mais image/vidéo « plus en phase » avec le doigt une fois zoomée, impression qu'elle pivote en déballage/déplacement. Aucun journal de geste ni capture annotée ; on ne peut pas confirmer une cause unique.
Mécanismes prouvés par inspection :
1. `CapCutMultitrackEditorV06.kt:540-555` additionne chaque `rotation` fourni par `detectTransformGestures` lors du drag/pincement : aucune séparation zoom/translation/rotation ou seuil anti-micro-torsion. Rotation indésirable plausible. `onRotate`/transition ROTATE restent des causes distinctes à éliminer.
2. Le pan `pan.x / size.width * 2` et le pan Y emploient l'AndroidView plein écran tandis que `AspectRatioFrameLayout` FIT, output frame et bandes masquent parfois une partie de ce repère ; après zoom, `graphicsLayer` scale/pivot modifient encore les coordonnées affichées. Source plausible du ressenti décalé, NON démonstration d'un défaut de matrice particulier.
3. Le changement `stablePreview=true` se produit dès le geste et change CompositionPlayer→ExoPlayer ; `directGestureRevision` écrit après 90 ms puis `directGestureActive=false` sans attendre que les doigts se relèvent. `setSelectedTransform` déclenche updatedAt/composition et éventuellement upsert keyframe pendant un geste. Risque de saut, latence/rebase, historique excessif et désalignement preview/export, à instrumenter. `EditorScreen` secondaire attend la fin du contact, asymétrie connue.
4. Fallback `graphicsLayer` transforme l'AndroidView entier, alors que l'export applique une matrice aux images vidéo normalisées et des effets de transition ; tests de correspondance preview/export après zoom/rotate inexistants.
Décision d'audit : NE PAS CODER à cette étape ; aucune modification du moteur, ni APK/release. Distinguer ces mécanismes observés du ressenti et de toute cause racine encore non confirmée. Le précédent incident `UnsupportedOperationException` concernait la Surface de lecture et ne prouve rien sur la fidélité des gestes.

## FABVID-GEST-005 — mise en œuvre v0.25.5, validation téléphone encore attendue
Le rapport de Fab v0.25.4 (drag décalé après zoom, rotation imprévue) a déclenché ce correctif, pas un changement des fichiers médias. Causes structurelles observées : `detectTransformGestures` ajoutait l'angle du pincement, pan sur grande AndroidView au lieu du frame FIT, `graphicsLayer` transformait la même vue que `pointerInput`, sauvegarde toutes les 90 ms et `updatedAt` susceptible de reconfigurer le lecteur pendant le geste. Correction de code : calque tactile frère non zoomé et frame sortie centré, math de compensation centre du pinch, rotation tactile momentanément verrouillée (rotation existante intacte), commit unique au relâchement. Effets secondaires à surveiller : réinitialisation du fallback ExoPlayer lors du premier mouvement ; effets Media3 et animation de transitions peuvent différer du calque ; pinch aux limites ±2 position et scale 0.25..4 ; résolution de l'ambiguïté UX zoom+rotation demandée par Fab. La cause visuelle exacte n'est pas prouvée sans capture/essai instrumenté. Aucun test physique effectué lors du commit.

## FABVID-GEST-006 — correction de compréhension de la rotation, 22/09/2026
Après v0.25.5 Fab précise qu'il voulait un différentiel INITIAL significatif, **pas** supprimer le twist à deux doigts. La solution « rotation uniquement via curseur » était une interprétation trop restrictive de l'audit. Correction v0.25.6 : porte d'intention 10° net signé, franchissement sans ajouter l'angle accumulé ni le delta de franchissement (sinon saut), rotation active sans nouvelle barrière jusqu'au changement de doigts ; zoom/pan conservent leur comportement v0.25.5. Math du zoom améliorée : ancien ancrage `(1-scale)*offset` ne compensait pas un nouvel angle ; nouvelle transformation inverse/forward intègre zoom + rotation + pivot afin de garder le centre sous les doigts. Pas de modification export ni fichier vidéo.
Risque non clos : 10° peut être trop dur/facile selon utilisateur, latence/perte du contact ou geste multitouch, orientations non uniformes et position clampée ; vérifier sur vrai Android. Pas de preuve d'un bug résolu au seul succès CI. La décision est réversible en conservant le module `PreviewGestureMath`.


## FABVID-MULTI-007 — symptôme signalé avant import EASYCUT v0 (22/09/2026)
Dans FabVidEdit 0.25.6, Fab rapporte : clip 1 correctement tronqué à l'export, seconde vidéo sur V2 absente visuellement (noir après le clip 1), aperçu de secours « V1 • PISTE ACTIVE » alors que V3 est sélectionnée, durée perçue comme non rallongée. Capture et export fournis par l'utilisateur. Hypothèse NON confirmée : lacunes/prolongements opaques et ordre des séquences vidéo dans `CompositionFactory.multitrackSequences`, en plus de `previewClipFor` monoclips ; exclure aussi mauvais positionnement temporel, fin prématurée et source illisible. `VideoProject.durationMs` utilise déjà le maximum des fins : ne pas remplacer cette logique sans démonstration. Aucune correction appliquée pendant le transfert, aucun test téléphone prétendu.


## FABVID-MULTI-007 — correctif candidat EASYCUT v0.0.1 (22/09/2026)
Observé en code v0 : `CompositionFactory.multitrackSequences` crée des gaps vidéo d'une piste haute avant/après son clip ; la première séquence composite s'affiche au premier plan. Ce type de gap peut générer un frame vide opaque et couvrir le clip d'une autre piste, cohérent avec l'écran noir du MP4 transmis ; cause racine définitive à vérifier avec une exportation réelle et comparaison d'images. Correctif : réglage du compositeur Media3 (alpha=0 pour les trous ; alpha utilisateur quand le clip est actif), sans retirer les gaps utiles au positionnement. Autre défaut prouvé : `previewClipFor` retombait sur un clip sélectionné ou le clip le plus proche lorsqu'aucun clip n'était actif et suivait l'horloge du clip au lieu de celle du projet ; remplacer par le plan actif le plus haut et une horloge de secours sur les lacunes. `VideoProject.durationMs` n'avait pas le défaut soupçonné : la formule du maximum a été préservée et encadrée de tests. Nouveaux risques à vérifier sur téléphone : Media3 shader alpha à la frontière du clip, reprise du fallback, synchronisation des pistes audio, rafraîchissement du plan/opacité, dimension canvas et sortie 4K/HEVC. Tests JVM fournis, pas d'export instrumenté local.


## EASYCUT-IMPORT-008 — erreur FFprobe remontée sur téléphone (22/09/2026)
Sur EASYCUT 0.0.1, Fab a transmis capture + rapport : import refusé à « Inventaire des pistes (métadonnées courtes)… » ; `FfprobeMediaInspector.inspectInput:34` ; FFprobe writer JSON « Failed to open output '' / ',�Z4?' » (No such file or directory / Read-only file system), répété à plusieurs tentatives, sans arrêt Android identifié. `session.output` contenait un message natif, pas l'inventaire JSON. Le projet existant s'ouvrait néanmoins : ne pas diagnostiquer à tort une erreur d'ouverture du média, d'identité ou de permissions Android, ni proposer de désinstaller et risquer les projets.
Cause exacte native NON définitivement démontrée : l'ancienne commande ne fournissait pas d'option `-o` explicite, et les appels FFprobe sur plusieurs points de l'application n'étaient pas sérialisés. Correctif candidat 0.0.2 : fichier JSON temporaire privé explicitement adressé, taille contrôlée, suppression `finally` et verrou entre sessions FFprobe ; mode scanner `-o` conservé. Une commande d'inventaire structurée et des tests JVM sur arguments/section critique ont été ajoutés. Vérifier après installation Android que les imports auparavant bloqués fonctionnent, et que le scan de résolutions, les miniatures, le montage et l'export ne régressent pas. Si l'erreur subsiste, collecter la valeur de code de retour et identifier la cause dans le fork ffmpeg-kit, pas tenter un effacement aveugle du cache ou des données.


## EASYCUT-CRASH-009 — preuve terrain et cause de crash (23/09/2026)
Fab rapporte sur EASYCUT 0.0.2 : START puis IMPORT_START, inventaire et `SCAN_KEYFRAMES` échouent avec `code=1 bytes=0 Argument <path> provided as output filename, but <octets corrompus> was already specified` ; malgré cela préparation V1/A1 et `OPEN_PROJECT clips=1` à t=1790110038145. Ensuite `VIDEO_SURFACE_BIND` UnsupportedOperationException `class android.view.Surface. Use CompositionPlayer.setVideoSurface()` depuis `FabVidVideoTextureView.kt:35`, suivi d'une NPE NON RATTRAPÉE du thread main en `CapCutMultitrackEditorV06.kt:557` (accès `fallbackClip!!` alors que `fallbackVisual != null`), puis redémarrage `PREVIOUS_EXIT Crash Java/Kotlin`. L'ordre temporel prouve que le scan, qui renvoie fallback, n'était pas lui-même l'arrêt Java ; la NPE est fatale.
Référence open-source : androidx/media issue #3164 reproduit même exception de `setVideoSurface(surface)` ; l'API spécifique `CompositionPlayer.setVideoSurface(surface, Size)` évite le chemin SimpleBasePlayer. Docs FFprobe : `-o` définit la sortie ; ici `-o` EXISTE déjà dans scanner et l'erreur dit précisément sortie DÉJÀ spécifiée, donc NE PAS redéduire absence de `-o` ni affirmer cause exacte sans debug du fork ffmpeg-kit maintenu. Contournement de sécurité pour média >256 Mio ou longueur inconnue : ne pas lancer le scan optionnel, garder les métadonnées de pistes inventoriées. Il reste à auditer le parsing natif des arguments (fork), puis restaurer le scan dynamique. Tests JVM du cas mono piste + audio ajoutés ; crash Surface/Compose nécessite essai instrumenté sur téléphone.


## EASYCUT-TIMELINE-010 — nouvelles preuves terrain (23/09/2026)
Fab montre V1 0–~3 s, V2 ~4–10 s, aperçu simplifié V2 et son premier clip anormalement zoomé uniquement dans la sortie. L'export transmis dans cette conversation dure ~10,204 s, 720×720 HEVC, SAR 1:1, avec une image presque uniformément floue pendant les premières secondes avant un second plan reconnaissable. La capture présente un montage avec sources indiquées portrait 720×1280. La capture du panneau BASE à S=1 concerne le clip V2 sélectionné, PAS la preuve de l'état complet de V1. Origine source non fournie ; causes à discriminer : images déjà grossies dans le média, mauvais rapport d'aspect projet, information de rotation ou dimensions décalées, changement de résolution dans un gros source non scanné, effet/matrice/transition non affichés dans l'aperçu simplifié, bug Media3. Ne pas écrire « hérité de base » comme cause prouvée.
Autre défaut signalé : difficulté d'amener V2 sur piste principale V1 en bout du clip existant. Le modèle `moveClipOnTimeline` accepte bien index 0 et règle les collisions ; l'UI lançait `onSelectClip`+seek dès `onDragStart` lors d'une recomposition avec poignées, facteur plausible (non démontré) d'interruption tactile. Solution ergonomique et sûre : « → Suite V1 » en plus d'appui long déplacé, avec tests du modèle contre duplication, collision, durée et respect des autres clips. Valider sur téléphone si le drag vertical est corrigé ; cette version n'affirme pas l'avoir reproduit mécaniquement.


## EASYCUT-WYSIWYG-011 — rectification du diagnostic et changement ciblé
Fab confirme : 321 % visible sur le keyframe de V1 est sa volonté. Ne jamais qualifier cette valeur de keyframe parasite (diagnostic antérieur ERRONÉ), ne pas lui conseiller un reset. MP4 transmis `13978.mp4` : HEVC 720×720, durée 10.203696 s ; à 1 s frame marron uniformément extrêmement rapprochée alors que capture `13980.jpg` du preview simplifié à 2 s montre une petite zone de vidéo près du bord supérieur, texte S 3.22. Ni ce MP4 ni la capture ne contiennent le fichier source original, donc un éventuel défaut restant dans l'encodeur/Media3 n'est pas exclu.
Cause structurelle IDENTIFIÉE par lecture du code : `AspectRatioFrameLayout` imposait `outputRatio` au TextureView MATCH_PARENT et Compose `graphicsLayer` zoomait l'AndroidView complet, alors que l'export FIT une source portrait avant transformation. Le preview ne reflétait pas le même frame ni le même pipeline d'échelle ; les données persistées restent valides. Correctif candidat : TextureView transform native avec FIT source/canevas et modèle fixe pour X/Y/zoom/rotation ; suppression des scales externes Compose en mode fallback ; tests géométrie portrait carré à 321 %, portrait non carré à 380 %, paysage carré et pivot. AUCUNE affirmation d'export définitivement corrigé sans comparaison de vrais frames sur le téléphone ; si différence subsiste, inspecter les matrices Media3 GPU, orientation et ratio réels/proxies ainsi que transitions.

Contrôle supplémentaire du même audit : l'UI calculait précédemment son ratio via width/height brut alors que l'export utilisait une extension tenant compte des rotations 90/270°. L'extension ratio source est centralisée ; ne pas en déduire qu'une rotation de keyframe à 2° vaut orientation métadonnée 90°.


## EASYCUT-PREVIEW-012 — régression signalée après v0.0.5 (23/09/2026) — OUVERTE / NON CORRIGÉE
Retour Fab : sur la dernière version EASYCUT v0.0.5, plus de problème constaté à l'export sur son montage, MAIS prévisualisation qui ne suit plus en temps réel / n'arrive pas à calculer suffisamment vite. Comparaison historique : v0.0.4 avant alignement FIT/TextureView v0.0.5 ; aucun rapport d'exception fourni pour cette lenteur, ne PAS la confondre avec NPE v0.0.2 ni FFprobe 1 Go. Ne PAS réouvrir « export zoom géant » comme défaut actuel confirmé ; maintenir seulement test de non-régression multipiste/source portrait/keyframe 321 % et future échelle volontaire 380 %.
Audit statique exact : `CapCutMultitrackEditorV06.kt:346-383` publie `currentPositionMs` sur `delay(80L)` et a `fallbackClip?.id` dans les clés de relance ; `fallbackVisual` dépend du temps `223-229` ; `AndroidView.update` `530-544` réapplique `FabVidVideoTextureView.applyPreviewTransform` à chaque recomposition, même quand matrice inchangée ; `FabVidVideoTextureView.kt:65-92` fabrique `Matrix` puis `setTransform`, dont Android précise qu'il invalide/redessine la TextureView. `LaunchedEffect(...project.updatedAt...)` `387-424` resynchronise avec `seekTo` et audio ; `onSurfaceTextureSizeChanged` relie à nouveau Surface après nouvelle taille. Ces mécanismes sont des suspects vraisemblables, PAS une cause prouvée. La cadence des keyframes/UI plafonne structurellement à ~12,5 évaluations/s dans la boucle, sans établir la cadence native du décodeur.
Méthode de reproduction/acceptation à la prochaine intervention : compter des images effectives et mesurer le décalage entre timestamp affiché, position ExoPlayer et horloge monotone pendant 30 s, profil UI jank/temps recalcul matriciel et binds, mesurer startup seek/clip boundary/audio sync. Aucune « optimisation » ne doit redonner l'export surzoomé ni supprimer les transformations volontaires, déplacer clips ou dégrader son/qualité MP4. Choisir stratégie après mesures : séparer horloge/décodage/édition, n'appliquer matrice que si valeurs changent, scrubbing dédié, éviter les seeks/reattach aux récompositions sans raison, fréquence de rendu adaptée au support. Tests JVM seuls insuffisants : vérification Android réelle, Media3/TextureView GPU et gros fichiers indispensable.
## EASYCUT-EXPORT-SMALL-013 — demande de nouvelles options (23/09/2026) — PLANIFIÉE / NON CODÉE
Fab demande bouton « + Autres » sur ligne cadence, FPS 1–5/10–12/15/20/24/25–60, curseur libre 1..60 ; résolutions 480p et autres petites résolutions, codecs et profils débit/audio, taille estimée et mode économique/qualité. Les chiffres des débits sont des suggestions de tests, pas des garanties ni exigences encodeur prouvées. Attention documentée : `setFrameRate` Media3 est un maximum pour vidéo déjà cadencée, pas un convertisseur garantissant exactement le FPS choisi ; exiger inspection MP4 et préserver durée audio/vidéo. Aucun code, nouvelle release ou test téléphone généré par cet audit.


## EASYCUT-PREVIEW-012 — intervention v0.0.6 : causes vérifiées et limites
Cause avérée STATIQUE de la lenteur de transformation : boucle UI 80ms et `AndroidView.update` exécutant `TextureView.setTransform` même pour matrice identique ; `LaunchedEffect(project.updatedAt,selectedClip.trackIndex)` réexécutait `syncFallback`/seek et `syncStableAudio` sur montage visuel/keyframe ou sélection de piste ; la boucle horloge redémarrait sur fallbackClip.id. Changement v0.0.6 : geste appliqué immédiatement au TextureView, animation matrice native ~33ms, UI plus lente ~80ms, matrice cache + invalidation correcte après resize, identité média discriminante, instrumentation locale PERF. Ces modifications sont candidates et NE PROUVENT PAS encore que l'appareil peut décoder un grand flux en temps réel ; une source de 1Go peut rester lourde. L'export que Fab jugeait bon sur son montage v0.0.5 reste inchangé ; vérifier explicitement zoom utilisateur / transitions / V1-V2 sur MP4 v0.0.6.
Les métriques frames de TextureView sont des événements de mise à jour de surface, distincts de la cadence clé Compose et d'un score de fluidité perçue. Si restent frames faibles pendant geste, envisager proxy 480p/360p créé en tâche préparatoire avec espace libre et sans modifier original ; ne PAS annoncer un downscale live arbitraire sur ExoPlayer. Source GPU / audio drift / seek natif réels non mesurés dans CI.

Risque traité à l'intégration : premier geste depuis le mode CompositionPlayer pouvait zoomer une image composite avant rebinding différé de l'ExoPlayer. La bascule syncFallback+bind est déclenchée UNE FOIS avant `applyPreviewTransform` ; contrôles playback restent en pause durant le geste.

## EASYCUT-RECOVERY-013 — diagnostic après retour « certaines vidéos seulement »
Défaut observé par Fab : toutes les vidéos ne présentent PAS le même ralentissement de preview. Ce fait invalide une simple cause systématique « zoom CPU trop lent ». Facteurs discriminants vérifiés statiquement : première bascule CompositionPlayer→ExoPlayer au pinch, syncFallback prepare+seek, disponibilité de surface/frame ; H.264/HEVC extraits en TS par préférence historique ; première vidéo pouvant être à rotation/résolution ou GOP différents. Aucune causalité native/format prouvée sans fichiers problématiques et essais téléphone.
Correctifs candidats lot v0.0.7 : freeze de la frame antérieure et transformation relative durant transfert surface ; récupération de frames source basse/moyenne définition uniquement si snapshot absent ; MP4 préféré au TS lors de futurs imports H.264/HEVC, TS/MKV en repli ; inventaire FFprobe stdout borné après échec -o ; sérialisation ciblée des appels FFmpegKit probe/remux/miniatures. Ne pas interpréter « source déjà importée » comme réimport automatique et ne pas affirmer que le changement de conteneur corrige tout décodeur.
Risques résiduels : snapshot initial peut contenir plusieurs pistes, récupération de frame Android peut échouer, source de contenu variable et codec corrompu restent difficiles, quelques vidéos TS anciennes restent telles quelles, output du scan de résolution pour source >=256 Mio encore désactivé à dessein ; UI/MP4 exact, qualité visuelle et course onRenderedFirstFrame nécessitent essai instrumenté appareil. Scénarios JVM ne reproduisent PAS le GPU/Codec2/exynos. Aucun test téléphone effectué lors de cette intervention.

## EASYCUT-RECOVERY-014 — retour Fab v0.0.7
Preuve journal exacte : STAGE SCAN_KEYFRAMES → ERROR FFPROBE_KEYFRAME code=1 bytes=0 « Argument '/.../resolution-frames-...txt' provided as output filename, but '��C?Z�' was already specified » ; malgré cela STAGE préparation vidéo/audio puis OPEN_PROJECT clips=1 ; Android exit none. ERREUR d'interprétation précédente rectifiée : 4 frames vs 182 matrices dans une mesure ne PROUVE PAS un décodeur bloqué car geste met ExoPlayer en pause et frame freeze dessine ailleurs.
Cause vérifiée : le repli stdout v0.0.7 était seulement dans FfprobeMediaInspector pour inventaire court, alors que ResolutionFrameScanner conservait -o natif pour source <256 Mio. Changement : garde fail-open globale tant que writer natif non validé ; avertissement fonctionnel en todo. Ne pas déclarer que FFprobeKit lui-même est corrigé.
Lacune vérifiée : pont frame freeze ne s'initialisait que dans !stablePreview, pas quand ExoPlayer déjà mode simple et qu'aucune frame n'a encore été présentée. Ajout de garde par surface et récupération ponctuelle sans seek ; ne pas capturer/freeze sur simple mode déjà correctement rendu, car un player paused peut ne pas produire de nouveau firstFrame pour retirer overlay.
Tests sur appareil toujours à faire : petite source dont native -o échouait, sous-projet résolution variable, 1 clip déjà simple, premier pinch multipiste, images identiques, rotation/keyframes, audio, export WYSIWYG ; les compteurs ne remplacent pas une observation visuelle.

## EASYCUT-PREVIEW-015 — hypothèse contrôlable après retour « ça marchait avant »
Journal v0.0.8 de Fab : mode=multi, 1 clip, frames=120, matrices=2, frozenDraws=0, recovered=0, freeze=false, waiting=false, ageMs=1259, playing=false, state=2 (BUFFERING), binds=1 ; aucun arrêt Android/erreur. Cela N'EST PAS une preuve de panne globale du décodeur, ni de réussite du pinch. Le code v0.0.8 démarrait CompositionPlayer dès un clip simple, puis au premier geste pause/syncFallback(prepare+seek)/transfert de surface vers ExoPlayer. Le gel d'image était un palliatif.
Diff v0.0.4 inspecté : il commençait lui aussi en mode multi sur un clip simple ; v0.0.9 n'est PAS une restauration littérale du code 0.0.4. Hypothèse testable : retirer la transition du premier pinch pour le cas simple peut restaurer la continuité. Nouveau routage mono-vidéo via ExoPlayer dès projet ; overlays/filters/texte restent en composition.
Risques non résolus : différences de rendu entre Media3 composition et ExoPlayer sur certains flux/rotations/effects, preview simple de pistes complexes avec spans inégaux déjà connu, frames=0 normales en pause, FFprobe dynamique neutralisé ; aucune validation appareil de cette hypothèse au seul succès CI. Version v0.0.8 conservée pour comparaison, ne pas effacer données ni rétrograder via désinstallation.

## v0.0.10 — correction explicite d'attribution de bug / causalité vérifiée
Fab a fourni capture d'écran v0.0.3 (image bien visible, R -70°, S 0,66) puis PRÉCISÉ : « elle cumulait 2 zooms À L'EXPORTATION SEULEMENT, la prévisu était bonne ». Ne pas reproduire l'interprétation erronée « aperçu v0.0.3 double zoom ».
Diff confirmé : 0.0.3 faisait scaleX/scaleY/rotationZ/translationX/Y/pivot sur graphicsLayer Compose de l'AndroidView simple ; 0.0.9 applique ces gestes sur la matrice TextureView, graphicsLayer ne garde qu'alpha. Logs 0.0.9 : import et projet OK ; mode=simple, state=3, matrices=140, mais Fab observe aperçu KO, ce que les compteurs ne contredisent pas.
Hypothèse corrigée : rendre la transformation visuelle à graphicsLayer Compose V0.0.3 en gardant export Media3 corrigé et routage ExoPlayer mono-vidéo v0.0.9. Pont bitmap ponctuel v0.0.7/8 plus appelé pendant pinch ; prévoir observation sur flux réellement difficile sans supposer que l'ancien snapshot règle les frames noires. Pas de réécriture du système de sortie.
À tester sur Samsung : zoom/dézoom R -70° S0.66, translation/doigts, lecture pause/reprise, effet/texte multi, MP4 final UNE SEULE fois zoomé, audio, mémoire. Pas de statut « réparé » avant retour visuel.

## EASYCUT-EXPORT-MOVE-017 — régression après déplacement de la vidéo 2 (23/09/2026) — RÉSOLUE / VALIDÉE FAB v0.0.14

**Preuves rapportées par Fab :** dans EASYCUT 0.0.10, la prévisualisation est enfin « nickel ». Premier MP4 vidéo 1 + séquence karaté vidéo 2 avant déplacement : correct. Après avoir glissé-déposé la deuxième vidéo **une ligne au-dessus** sur la timeline puis réexporté : le zoom indésirable apparaît dans l'export de la vidéo 1, tandis que les transformations/keyframes attendues de la vidéo karaté 2 ne sont pas reproduites correctement. Capture d'écran et fichier MP4 communiqués dans la discussion. Ne pas requalifier en panne de prévisualisation : celle-ci et le premier export constituent désormais les références stables.

**Hypothèse technique, pas cause prouvée :** `moveClipOnTimeline` change le projet du séquentiel au `TimelineMode.MULTITRACK`, et `CompositionFactory.multitrackSequences` organise les vidéos par piste. Examiner les `timelineStartMs`, trims, source-time et clip-time des keyframes, priorité de superposition, `AnimatedTransformEffect`, ainsi que l'identité du clip auquel chaque effet appartient. Le constat ne permet PAS d'affirmer un transfert effectif de keyframes d'un clip à l'autre ; la superposition peut aussi donner une illusion de mauvais zoom.

**Erreur de suivi constatée :** l'assistante avait copié presque tout ce rapport, ses symptômes et son hypothèse directement dans `todo.md`, puis annoncé « 104 cases à faire » en mélangeant cases anciennes de versions successives, doublons, fonctionnalités réellement attendues et diagnostics. Contraire à l'organisation FAB Copilot : ici l'histoire/les faits/hypothèses ; dans `todo.md`, une action et sa validation. Corrigé par rangement documentaire seul, sans toucher à la prévisualisation, à l'export, aux APK ou aux projets. Aucun autre petit chantier ne doit être considéré perdu sur la seule base du compteur de cases.

**Suivi historique au 23/09 avant v0.0.14 :** les actions indiquées ont été exécutées (temps local V2 en v0.0.13, toile fixe export V1 en v0.0.14). Fab confirme ensuite explicitement le scénario résolu en v0.0.14 ; voir la clôture datée en fin de fichier et `todo.md`.

## FAB-MISSION-001 — rectification du classement et de la méthode (23/09/2026)
Après récupération du TODO, Fab a précisé que les demandes cadence/résolution/compression et trois fonctions GET ERR constituaient ses **ordres de mission persistants**, pas les sous-tâches internes de l'agent. Fab a proposé un fichier dédié : `ordres-de-mission.md`. La proposition intermédiaire de simplement étiqueter des missions `[FAB]` à l'intérieur de `todo.md` était insuffisante. `todo.md` reste le plan de l'agent, `topo.md` une carte rapide utile à l'agent. Règle appliquée au dépôt FAB Copilot et à ce projet documentaire ; **aucune de ces fonctionnalités n'est codée**, aucune APK nouvelle, et le bug d'export de piste n'est pas supposé corrigé. Les missions conservées ne peuvent être annulées ou dépriorisées par l'assistant.

## 23/09/2026 — correction de compréhension FAB-JOURNAL-COPY-007 (pas un bug de preview)
- Fab rectifie la suggestion de balises HTML dans l'affichage : **couleurs visuelles uniquement à l'écran**, **balises HTML littérales dans le COPIER uniquement**. Rattacher cette clarification à `ordres-de-mission.md` et au comportement d'un seul GET ERR partagé, non à une fonctionnalité générale de FAB Copilot ni à une nouvelle erreur d'export.
- Risques contrôlés par conception : traces contenant `<`, `>`, `&` HTML-échappées lors de la copie ; un timeout bloquant reste rouge même si un message mentionne « repli » ; vidage confirmable des lignes anciennes sans effacer diagnostic d'arrêt ni médias.
- CI/JVM et test téléphone non encore rapportés ici : ne pas annoncer résolution Android avant vérification du pipeline et essai de Fab. L'aperçu v0.0.10 et premier export sont des acquis, `EASYCUT-EXPORT-MOVE-017` demeure ouvert et non modifié.

## 23/09/2026 — clarification de Fab et instrumentation limitée v0.0.12
Fab rectifie explicitement : il n'y a AUCUN problème d'aperçu EasyCut ; il est parfait. Le défaut concerne EXCLUSIVEMENT le fichier MP4 : première vidéo agrandie excessivement et images-clés vidéo 2 non reproduites. Éviter « pas seulement un problème d'affichage ». Fab demande d'agir seulement sur l'instrumentation GET ERR et davantage de cadences dont 10/15, sans promettre un audit ultérieur ni coder de correctif visuel maintenant.
Hypothèses non tranchées d'EXPORT-MOVE-017 : signification de Media3 presentationTimeUs dans les effets, passage SEQUENTIAL→MULTITRACK, géométrie d'entrée/sortie, mise à l'échelle ou ordre des séquences et compositeur. Elles restent hypothèses ; ne pas les afficher comme causes confirmées.
V0.0.12 trace valeurs avant l'export, matrices RENVOYÉES, alpha/compositeur et rapport encodeur. Le journal ne mesure pas les pixels du MP4 ; comparaison du vrai fichier et tests Android nécessaires. Surcoût potentiel des écritures échantillonnées pendant l'export à surveiller. Aucune modification de la transformation existante.

## 23/09/2026 — résultat réel de GET ERR 0.0.12 et correctif limité v0.0.13
Fab donne un export H264 MULTITRACK de 13.404s, V1 start0/source 720x720, V2 start5871/source 720x720 et ratio de canevas 9:16. V2 keyframes=1313/2104/3024 ms, première MATRIX au projet 5871ms donne localMs=5871, kfPrev=3024, next=END, identité ; suite clamp 7533 → identité jusqu'au bout. Cause démontrée : getMatrix confond temps projet et temps source-local sur la seconde piste. Conversion ciblée à l'export seulement (+brightness), test de la keyframe 2104ms attend zoom 2.0855768 et rotation 91.86181. V1 matrices identité 0–2s, zoom visible ne vient pas de ces matrices ; ne pas inventer la cause du cadre V1. Ajouter diagnostic tailles réelles compositeur, V1 reste ouvert.
267 frames / 13384ms pour 12fps max demandé : ~20fps moyen signalé, preuve de plafond insuffisant une fois pistes mélangées, pas de preuve CFR fichier sans MP4. FrameDropEffect global export uniquement ; confirmer sur appareil. Notice GET ERR vidée faussement rouge car contient « erreur » : classer INFO sans toucher aux erreurs réelles. Toute régression de l'aperçu est interdite ; preview 0.0.10 nickel. Tests CI non assimilables à validation de MP4.

## 23/09/2026 — Fab transmet GET ERR 0.0.13 et MP4 14026.mp4
V2 presque conforme à l'aperçu selon Fab ; ne pas défaire correctif de son temps local. V1 reste fortement recadrée : vidéo 14026.mp4 720x720, 152 images pour ~13.66s, gros aplats beiges sur premières secondes puis V2 nette. GET ERR : inputSizes=[16x16 (gap V2),720x1280 (V1)] → DEFAULT output=16x16 pendant début/V1 ; après 6118ms entrée V2 réelle, output=720x1280. Ce choix initial dépend de la première piste enregistrée (V2 avec gap) et non du projet 9:16 ; fournit un mécanisme concret de l'agrandissement/rognage V1 alors que sa matrice n'est pas doublement zoomée. V0.0.14 impose output de projet constant pour export, sans toucher preview. A CONFIRMER VISUELLEMENT SUR MP4 produit avant clore EXPORT-MOVE-017. Traces COMPOSITOR_SIZE répétées chaque image ont noyé BEGIN/MATRIX dans limite report 25k et coûtent I/O ; dédoublonner. Get ERR mentionne ancien Crash Java/Kotlin à date=1790194254617, étape audio 1/1, aucune erreur capturée ; export 0.0.13 à 22:11 finit ENCODE_DONE/GALLERY_OK : ne pas attribuer le crash ancien à l'export actuel, traiter comme incident distinct si reproduit. Ne pas toucher au lecteur preview parfait.

## 23/09/2026 — clôture explicite par Fab de EASYCUT-EXPORT-MOVE-017
Après installation et vérification de v0.0.14, Fab déclare : « Parfait ma douce tu peux cocher toute les cases liées à cet incident comme résolu, on est Parfait à ce stade, je poursuis le test et je reviens vers toi ». Statut du scénario connu de l'export multipiste V1/V2 : **RÉSOLU, VALIDÉ UTILISATEUR**. Correctifs en deux étapes : v0.0.13 pour le temps local de V2 et v0.0.14 pour taille de toile d'export stable face aux gaps 16×16 de V2 ; l'aperçu n'a pas été modifié. Le récit des échecs antérieurs reste comme historique, pas comme problèmes encore ouverts. La validation de ce scénario n'efface ni le crash Android ancien (sans cause attribuée), ni les demandes de compression/résolutions/cadences à tester spécifiquement, ni des tests futurs sur de nouveaux montages.

## 24/09/2026 — rectification des validations déjà apportées par Fab
L'agent avait classé cadences et GET ERR « à valider », y compris TRACE-008, alors que Fab avait déjà copié-collé des journaux HTML utilisés pour déboguer l'export. Fab précise : « Cadense ok. Get ok. Couleur et deuxième bouton ok vider ok html ok » ; le tracé doit seulement rester dans la liste AGENT en mineur pour son éventuel entretien. FPS-001, JOURNAL-004/005/006/007 et TRACE-008 sont donc VALIDÉS ; anciennes mentions « à valider » historiques. Aucun code touché.

## 24/09/2026 — mise en œuvre RES-002 et SIZE-003
Fab lance les deux missions rouges après validation V1/V2/FPS/GET ERR. Risques restants à tester sur smartphone : format 240–540p et repli encodeur HEVC, débit demandé parfois différent de réel, taille indicative et non promise, perte audio si remux échoue (erreur visible, pas de galerie mensongère). Aucun réglage ne doit convertir la musique stéréo en mono à l'insu de Fab ; canaux originaux par défaut, conversion uniquement explicite. Remux audio optionnel -c:v copy pour préserver vidéo et keyframes validées. Incident crash Android et scan FFprobe restent distincts. Tests MP4 et validation Fab en attente.

## CI v0.0.15
Compilation, tests, lint et publication GitHub Release v0.0.15 confirmés par run 35995268578 ; aucun échec de build constaté. Les risques MP4/son/encodeur listés précédemment restent des cas à tester sur appareil, pas des incidents prouvés.

## 24/09/2026 — retour terrain Fab sur EASYCUT v0.0.15
Fab confirme « Test ok » après lancement de RES-002 (petites résolutions) et SIZE-003 (export économique). Les deux missions sont **VALIDÉES PAR FAB** ; les avertissements techniques antérieurs restent des risques théoriques / tâches de robustesse, pas des bugs signalés par ce test. Aucun nouvel incident rapporté ici. Crash Android ancien et FFprobe natif neutralisé conservent leur historique séparé, sans rouvrir V1/V2, aperçu, cadence ou GET ERR. Aucune modification de code.

## 24/09/2026 — signalement source non chargée / FFprobe et pixels non carrés
Fab constate que certaines vidéos ne se chargent toujours pas avec un message d'erreur FFprobe ; il estime que les occurrences sont peut-être moins fréquentes, sans chiffres ni échantillon représentatif. Les causes ne sont pas déterminées ; ne pas déclarer que chaque échec import relève du scanner de résolution neutralisé, ni que le problème a disparu. Nouveau point : certains flux embarqués ont possiblement SAR≠1 (pixels non carrés) ; vérifier la présence et la fiabilité des métadonnées SAR/DAR, la normalisation déjà faite par le décodeur et la distinction proportion de pixels / rapport de toile / zoom créatif. Ne pas confondre avec le défaut de cadrage V1/V2 déjà corrigé et validé. Mission FAB-SOURCE-COMPAT-011 inscrite prioritaire par Fab, `code pas de suite`. Toutes les causes précises, la forme exacte du correctif de matrice et l'étendue de compatibilité restent à établir après autorisation de coder.

## 24/09/2026 — Fab précise que le lecteur est vraisemblablement correct, matrices keyframes suspectes
Correction explicite du récit initial : Fab dit « Le lecteur ça a l air de aller mais les matrice de transformations par kframe non ». L'assistante avait formulé trop largement une normalisation du flux vidéo avant la transformation. Ce n'est pas le problème confirmé par Fab : l'affichage non animé semble correct ; les proportions paraissent se dégrader lorsque l'on applique les matrices animées par images-clés. Hypothèse à examiner : décalage entre coordonnées de pixels encodés et coordonnées d'image affichée sous SAR≠1, pas cause déjà démontrée. Ne pas modifier le lecteur brut, ne pas double-corriger le SAR. FFprobe/compatibilité garde son suivi distinct sous la même mission. Pas de code à ce stade.
