# Media3 and Compose ship consumer rules. Keep the model names because projects are
# persisted as explicit JSON fields and are intentionally readable for migrations.
-keep class com.fabvidedit.app.model.** { *; }

