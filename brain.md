# brain.md — FabVidEdit

## Contrat rétabli (FABVID-REG-001)
Version de référence technique : branche `agent/v0.25-matrix-texture-preview` au commit `0cb6d330a88d8d654f4e5bc9edd50e5a523887d4`. Branche de correction : `agent/v0.25.1-fab-copilot-regressions`. Fab constate : aperçu bugué, gros fichiers mal optimisés, formats de sortie et paramètres régressés. Les symptômes précis sur téléphone restent à reproduire ; ne pas prétendre les avoir validés.

### Export (FABVID-EXP-001)
MP4 avec audio AAC ; H.264 disponible, H.265 proposé uniquement si encodeur matériel HEVC déclaré par Android. Résolution source/720p/1080p/1440p/4K ; cadence source ou 24/25/30/50/60 i/s ; débit auto ou 2/4/8/12/20/35/50 Mbit/s. Ces valeurs prédéfinies ne sont pas encore un sélecteur continu 2–50 Mbit/s ; amélioration suivie dans todo.md. Les réglages choisis par Fab doivent traverser UI → ViewModel → Transformer. L'aperçu léger ne doit jamais réduire la qualité d'export. Ne pas modifier silencieusement le projet original pour préparer une source d'export. Contrôler les possibilités réelles du matériel à l'export.

### Aperçu (FABVID-PRV-001)
Le lecteur doit préserver transformations et gestes en cours malgré les changements d'images-clés. Cible d'aperçu : 720p/30 images/s, 480p/30 sur appareil Android signalé low-RAM. La cause précise du bug de prévisualisation reste non prouvée ; vérifier sur téléphone les compositions multi-pistes, l'alignement aperçu/export, orientation et recadrage.

### Gros fichiers (FABVID-LRG-001)
Éviter de répliquer une source déjà gérée par le stockage privé de l'app. Éviter `faststart` pour les proxies temporaires TS de travail. Vérifier l'espace disponible avant un remux de proxy d'export ; nettoyer les proxies et copies après usage, sans effacer une source de projet. L'import externe non durable garde sa copie de sécurité ; une solution zéro copie pour URI externe demanderait validation de durabilité et de l'accès FFmpeg.

### Traçabilité (FAB-MEM-001)
Synchroniser brain.md, brainmap.md, debughistorical.md et todo.md avec tout changement de code dans le même commit. Publier une release APK/AAB seulement si build et livrables vérifiés ; tests téléphone distincts de tests CI.

### Import en flux et prévention du plantage (FABVID-LRG-002 / version 0.25.2)
Fab demande que les vidéos volumineuses soient lues en flux, sans accumuler les images en RAM : ne conserver que les métadonnées/quelques images de vérification de résolution, puis vider le travail temporaire. L'analyse FFprobe doit diriger sa sortie vers un petit fichier local jetable plutôt que vers `session.output` ; lecture ligne par ligne, histogramme et segments plafonnés, images-clés seulement quand le décodeur le permet ; suppression du fichier dans un `finally`. Le traitement des sources externes reste une copie durable effectuée par flux ; la séparation des pistes peut encore employer de l'espace disque, explicitement distinct de la RAM. Nettoyage du stockage sur Dispatchers.IO et fermeture de l'indicateur d'occupation même en cas d'erreur ; messages par étape. Ne jamais promettre un import sans crash tant qu'une vidéo réelle de 1 Go n'a pas été testée. Les cas de changements de résolution sans image-clé restent à étudier.

### Capture des fermetures silencieuses et sauvegarde des projets (FABVID-ERR-003 / v0.25.3)
Lorsque l'app se ferme sans message, éviter toute affirmation de cause sans rapport d'erreur et sans test téléphone. Tenir un journal local borné (192 KiB + traces limitées), privé et consultable/copier/partager depuis l'accueil par choix explicite de l'utilisateur ; méthodes `getLastError()`, `getLastStage()`, `getLastExitReason(context)`, `getReport(context)` ; erreurs Java avec `Log.e`, trace et stade d'import ; garder le gestionnaire Android normal après l'interception d'une erreur non capturée ; vérifier `ApplicationExitInfo` Android 11+ pour crash natif/ANR/low memory. Aucun SDK analytics ni upload automatique. Une mort SIGKILL/native peut survenir sans callback Java, mais le journal des dernières étapes et les motifs d'arrêt Android restent consultables à la réouverture quand disponibles. Le log partagé peut contenir des chemins/noms sensibles : avertissement et consentement utilisateur.
Ne jamais lancer un nettoyage automatique destructeur pendant qu'un clone ou une extraction est en cours, ni au démarrage en parallèle d'un import. Suspendre l'auto-reclamation de fichiers non référencés tant que les leases/transactions de fichiers ne protègent pas les imports, suppressions de projets et exports. Les fichiers temporaires que leur propre opération crée restent nettoyés explicitement dans leurs `finally`; ne pas supprimer les originaux ni données applicatives. Pour les gros médias, préférer un échantillonnage de fenêtres d'images-clés à une analyse native complète susceptible de saturer le décodeur ; noter qu'une transition entre fenêtres devient approximative et doit être testée sur vidéos à résolutions variables.

### Surface de lecture Media3 — FABVID-PRV-004 (v0.25.4)
Le GET ERR de Fab confirme que le fichier de 1 Go est importé et le projet ouvert avant le crash principal : `UnsupportedOperationException class android.view.TextureView. Use CompositionPlayer.setVideoSurface() for Surface output`. `PlayerView.player=CompositionPlayer` est interdit : Media3 route `PlayerView` vers `Player.setVideoTextureView` non pris en charge par `CompositionPlayer`. Brancher directement une vraie `Surface` au `CompositionPlayer.setVideoSurface` et libérer/débrancher sa surface avant son cycle de vie ; ne jamais partager sans débrancher le précédent lecteur. Si même la surface directe échoue selon version/codec, basculer en lecture de secours ExoPlayer et conserver la cause GET ERR, au lieu de laisser tomber le processus Java. Même contrat sur `EditorScreen`, pas seulement la vue multipiste. Vérification UI/device de lecture + image + pistes synchrones indispensable, compilateur CI insuffisant.

### CI FABVID-CI-004
Sur `AspectRatioFrameLayout` Media3, utiliser `setAspectRatio(ratio)` et vérifier la compilation Android réelle ; ne jamais déduire un APK du seul commit Git.

## FABVID-GEST-005 — contrat de gestes attendu (audit sans code du 22/09/2026)
Observation de Fab sur v0.25.4 : le zoom lui semble fonctionner, mais le déplacement de l'image/vidéo après zoom paraît décalé, « plus en phase » avec les doigts, et peut déclencher une rotation involontaire. Ne pas transformer cette observation en preuve de cause précise sans reproduction Android. Règle visée : un glissement à un doigt translate sans modifier l'angle ni l'échelle ; pincer à deux doigts modifie le zoom en restant ancré sous le centre du pincement ; une rotation nécessite une intention explicite distincte du simple zoom/drag (commande ou mode à valider avec Fab), sans bloquer la rotation créative volontaire. La réponse tactile doit rester 1:1 après zoom et sous différents formats source/projet, sans dérive due aux bandes noires, au pivot ou aux changements de lecteur ; gestes sur image et vidéo équivalents.
Ne pas confondre zoom de la timeline (`V06ZoomHeader`) et zoom/transform du contenu vidéo (`detectTransformGestures`). Ne pas inscrire une rotation issue de micro-torsions accidentelles comme mouvement volontaire. État gestuel isolé pendant le contact ; écriture des transformations et point d'annulation au plus une fois par geste terminé, sans animation/recadrage/retour parasite à mi-geste ; politique d'images clés conservée après validation. Prévisualisation stable et export doivent employer les mêmes conventions position/zoom/rotation/pivot, sous réserve des effets de transition. La conception de ce contrat est documentée, NON codée ni validée sur appareil.

## FABVID-GEST-005 — réalisation prudente v0.25.5 (accord Fab après audit)
Fab autorise le code mais demande de garder en vigilance la question encore floue du rapport zoom/rotation, sans figer définitivement l'ergonomie de la rotation créative. Contrat de cette itération : un doigt déplace sans rotation ; pincement/deux doigts zooment et déplacent en gardant autant que possible le point sous le centre du pincement ; ne pas générer d'angle pendant le zoom/drag. L'angle déjà réglé reste inchangé et le curseur Rotation du panneau Mouvement ainsi que les transitions ROTATE restent accessibles. Décision *provisoire/réversible* : pas de rotation à deux doigts dans v0.25.5, UX de twist intentionnel/sélecteur de mode à discuter et tester avec Fab, pas de remplacement silencieux par un angle arbitraire.
Calculer le pan dans les pixels de la zone de sortie visible, sans les marges noires ni l'inverse de l'échelle du lecteur, en conservant le zoom/pivot existants ; éviter la dérive après pinch à 2×/4×. Geste traité pendant le contact, pause/fallback conservés ; écrire le transform courant UNE fois à la fin du contact pour un seul historique et une keyframe éventuelle au temps initial du geste. Pas de publication déclarée stable avant tests téléphone photo/vidéo, zoom/cadre/rotation et export.

## FABVID-GEST-006 — v0.25.6 : rotation intentionnelle pendant pinch, décision explicite de Fab
Fab rectifie l'étape v0.25.5 : il n'a jamais demandé la désactivation définitive du geste de rotation. Deux doigts doivent toujours pouvoir zoomer, se déplacer **et** pivoter, mais seulement après un différentiel angulaire initial significatif : petit tremblement/twist d'une pince ordinaire ne doit pas tourner l'image. Choix provisoire testable : écart ANGULAIRE NET signé de 10° avec les DEUX MÊMES doigts stables ; le zoom/pan fonctionnent sans attendre. Quand le seuil est atteint, l'orientation actuelle devient **la nouvelle référence** : ignorer l'angle avant le seuil, ne pas appliquer le delta qui franchit le seuil (aucun saut), puis appliquer librement chaque variation d'angle subséquente sans imposer le seuil à répétition. À la perte ou au remplacement de la paire, réarmer. À un doigt, drag seul ; rotation existante conservée ; panneau Mouvement et transitions ROTATE inchangés. Le seuil de 10° est une hypothèse d'ergonomie à ajuster après essai Fab, pas un fait validé.
Un zoom/rotate combiné doit garder sous le centre du pincement le MÊME point de source, avec compensation de translation tenant compte de rotation/pivot/échelles X et Y et de leurs limites ; ne pas « réparer » la rotation en retirant le correctif v0.25.5 d'overlay tactile indépendant et commit unique au relâchement. Aucune promesse de fidélité réelle sans test Android et comparaison aperçu/export.


## EASYCUT v0 — migration publique, 22/09/2026
Base d'EASYCUT v0 = copie des sources du commit FabVidEdit `436649444f480c0aff21875de1a3a5e5d0be1618` avec consentement explicite de Fab pour publication publique. Historique privé, APK/AAB et logs historiques non recopiés. App visible EASYCUT, versionName 0.0.0, versionCode 33. `applicationId=com.fabvidedit.app.poc`, packages Kotlin et format des projets conservés pour éviter une migration implicite/destructive ; attention aux incompatibilités de signatures debug entre différents environnements de compilation. Aucune correction du moteur multipiste ni test Android réel attribués à ce simple transfert.
Contrat à appliquer ensuite : piste vidéo vide transparente visuellement, la fin projet égale la fin réelle maximale des médias, une coupe de clip n'écourte pas une autre piste sauf groupe lié explicitement ; ordre des plans personnalisable sans déplacement temporel ; aperçu fidèle/export alignés, aperçu de secours identifié comme simplifié. Les gestes v0.25.6 (zoom/pan immédiats et twist après intention 10°) sont conservés, seuil provisoire à tester.


## EASYCUT v0.0.1 — FABVID-MULTI-007 (correctif candidat, 22/09/2026)
- Un trou vidéo ou prolongement de piste n'est JAMAIS opaque : `VideoLayerPolicy.opacityAt` vaut zéro en dehors de `[timelineStartMs, timelineStartMs+outputDurationMs[`, et vaut l'opacité du clip à l'intérieur. Le `VideoCompositorSettings` applique la valeur à la séquence concernée au temps de présentation (µs → ms). L'ordre des séquences suit les niveaux V descendants ; conserver la taille par défaut du compositeur Media3. Un vrai média noir reste visible ; noir de trou ne masque pas V2.
- Deux commandes « Plan ↑ / Plan ↓ » échangent entièrement les pistes vidéo occupées, sans déplacer un seul clip dans le temps ni les pistes audio. Les commandes s'appuient sur `VideoLayerPolicy.moveLayer` ; le glisser-déposer spatial existant reste distinct. Un clip a désormais `opacity` [0..1] persistée avec valeur 1 pour projets historiques, réglable dans le panneau Mouvement.
- Aperçu robuste renommé « aperçu simplifié » et limité explicitement à la piste active visuellement la plus haute : aucune réutilisation du clip sélectionné/achevé à travers un trou. Transport de secours séparé du lecteur par clip pour continuer le temps du projet pendant le vide. Effacer visuellement une frame Surface obsolète pendant le vide. Le moteur complet reste à valider sur téléphone.
- `VideoProject.durationMs` calcule DÉJÀ le maximum des fins de tous clips et audio en multipiste : ne pas changer sa formule sans preuve ; tests de non-régression V3 0..3, V2 6..16 et prolongation à 18 secondes. Ne pas assimiler une lacune volontaire 3..6 à une coupe ripple.
- Aucune garantie empirique de correction MP4 ou preview Android au seul succès CI ; un test d'export réel du montage Fab reste requis. Ne pas changer l'identité Android, le code source FabVidEdit, les gestes ni la règle d'une sauvegarde par intervention.


## EASYCUT v0.0.2 — EASYCUT-IMPORT-008 (hotfix FFprobe, 22/09/2026)
- Retour réel Fab v0.0.1 : projet existant s'ouvre, mais nouvel import impossible à « Inventaire des pistes » ; FFprobe JSON échoue sur un chemin de sortie vide/illisible, erreur « No such file or directory » / « Read-only file system ». Ce n'est pas un crash Java ni une preuve que le média est endommagé.
- Inventaire court et local FFprobe désormais dirigé explicitement avec `-o` vers fichier JSON aléatoire privé `cacheDir/ffprobe_metadata` (taille max 4 Mio) ; lire ce fichier, pas `session.output` destiné aux diagnostics ; `try/finally` supprime le fichier temporaire, quel que soit le résultat. Les médias originaux ne sont pas modifiés. Les arguments structurés demeurent séparés et testables.
- Sérialiser toutes les exécutions FFprobe natives dans `FfprobeNativeGate` pour éviter les accès simultanés au writer/log natif ; ne verrouiller ni tout le workflow d'import, ni les exports FFmpeg. L'ancien scan image-clé reste borné à 64 Mio, streamé, et toujours avec fallback métadonnées en cas d'échec.
- Sont inchangés : applicationId, données projets, gestion des pistes/temps, moteur de composition, images, zoom/twist, réglages d'export. Le correctif est candidat tant que l'utilisateur ne l'a pas validé sur son téléphone ; le succès CI seul ne prouve pas le bon fonctionnement natif.


## EASYCUT v0.0.3 — EASYCUT-CRASH-009 (23/09/2026)
- Le rapport téléphone 0.0.2 établit trois défauts distincts : scan image-clé FFprobe code 1/path -o déjà spécifié, branchement Surface par l'API Player interdite pour CompositionPlayer Media3 1.11, et NPE main thread due à `fallbackClip!!` dans le rendu Compose. L'échec du scan n'était PAS la cause immédiate du crash : le pipeline a ensuite ouvert le projet (clips=1). `START` après redémarrage n'est pas le lieu du crash.
- `FabVidVideoTextureView` utilise `CompositionPlayer.setVideoSurface(surface, Size(width,height))` exclusivement pour CompositionPlayer ; ExoPlayer garde `Player.setVideoSurface(surface)`. Réémettre la liaison lors du changement de taille TextureView, et dissocier surface avant destruction.
- `CapCutMultitrackEditorV06` : remplacer la déréférence forcée du clip dans la couche Compose par opacité 0 en cas de clip absent, sans figer une ancienne image vidéo.
- `ResolutionFrameScanner` : désactiver temporairement et explicitement le scan FFprobe natif des images-clés pour médias >= 256 Mio ou de taille inconnue ; conserver les dimensions, temps et pistes déjà inventoriés, signaler `SCAN_SKIPPED_NATIVE_RISK`. Le compromis est qu'un changement de résolution en milieu d'un tel fichier n'est plus automatiquement découpé en segments jusqu'à correction native vérifiée sur téléphone. Pour petits fichiers, scan existant borné/fallback conservé.
- `VideoLayerPolicy.needsVideoCompositor` : custom `VideoCompositorSettings` seulement en présence de plusieurs pistes vidéo occupées ; une vidéo + plusieurs pistes audio ne doivent pas déclencher le graphe composite non applicable. Ce garde-fou traite le cas SingleInputVideoGraph, pas la validation d'un export complexe réel.
- Sans essai Android et export MP4 réussi de Fab, v0.0.3 demeure candidate. Identifiant d'installation et projets existants inchangés ; ne pas effacer les données pour réparer le crash.


## EASYCUT v0.0.4 — EASYCUT-TIMELINE-010 (23/09/2026)
- Retour Fab v0.0.3 : premier clip sur V1 paraît démesurément zoomé à l'export, bien que l'aperçu ne montre aucun zoom demandé ; deuxième clip sur V2 s'affiche normalement. L'export partagé (fichier 13978.mp4, environ 10,2 s) contient une première séquence extrêmement agrandie/floue, puis une deuxième séquence identifiable. La vidéo d'origine du premier clip n'est pas fournie : ce constat NE PROUVE PAS une transformation de base héritée. Conserver `VideoClip.transform`, transitions, métadonnées de rotation et `Presentation.LAYOUT_SCALE_TO_FIT` tant que la source et les propriétés du projet ne sont pas comparées. La lecture Media3 `Presentation` distingue FIT (sans recadrage) de FILL_WITH_CROP ; ne pas masquer la panne par un scale arbitraire.
- Un clip d'une piste V2 doit pouvoir être déplacé sur V1 à la suite du dernier clip de V1, sans duplication, sans changer le début des autres clips ni leur plan. Règle `endOfVideoTrack(0, excludedClipId)`, puis `moveClipOnTimeline` ; la fonction utilisateur utilise `FabVidEditViewModel.moveClip` pour respecter aussi les liens audio A/V verrouillés. Bouton accessible « → Suite V1 » en complément au glisser-déposer, sans mélanger `Plan ↑/↓` (ordre visuel) avec `déplacer vers V1` (piste de montage).
- Le geste de déplacement à appui long n'entame plus un seek/recomposition du clip dans `onDragStart` : sélection à la fin, l'appui simple continuant de sélectionner. Durée du projet se recalcule avec les fins des clips (pas de longueur arbitraire de la première piste). Ne pas déclarer le cadrage corrigé tant que la source d'origine n'a pas été comparée à l'export.


## EASYCUT v0.0.5 — EASYCUT-WYSIWYG-011 (23/09/2026)
- Fab confirme explicitement que l'échelle 321 % de V1 est VOLONTAIRE, et que ce n'est pas un keyframe parasite. L'aperçu montre un cadrage souhaité mais le MP4 précédent en sort un autre, fortement suragrandi : ne pas réinitialiser les keyframes ou diviser artificiellement les échelles pour maquiller le résultat.
- Défaut de géométrie confirmé dans le code : le preview de secours ExoPlayer rendait sa texture directement dans le conteneur de ratio du PROJET (TextureView MATCH_PARENT), puis `graphicsLayer` transformait le CONTENEUR AndroidView ; Media3 export fait au contraire `Presentation.LAYOUT_SCALE_TO_FIT` du MEDIA vers le canevas avant d'appliquer la matrice dans le frame. Pour un portrait 720×1280 en projet carré, ces référentiels sont incompatibles ; impossible de garantir WYSIWYG en utilisant le cadrage de secours ancien comme référence de l'export.
- Changement prudent : le preview simplifié applique FIT calculé depuis le rapport de SOURCE puis scale/rotation/pivot/pan EXACTS déjà enregistrés en pixels du cadre, directement à la texture native `TextureView.setTransform`. `graphicsLayer` parent ne modifie désormais que l'alpha. Débrancher le transform (matrice identité) en mode CompositionPlayer pour éviter tout double zoom. Actualiser la transformation quand la surface apparaît ou change de taille.
- Le modèle `VideoClip.transform/keyframes`, le pourcentage sauvegardé, le moteur `CompositionFactory` d'export, ses transitions et le placement temporel sont INCHANGÉS : conserver le choix de Fab (321 %, 380 %, etc.), en montrant le comportement géométrique réel à l'écran plutôt qu'en modifiant la vidéo à l'aveugle. La validité visuelle 1:1 avec le MP4 exige le contrôle sur appareil (source brute et export test) ; les tests JVM vérifient la géométrie FIT/échelle/translation/pivot sans prouver le rendu GPU.

Complément cycle v0.0.5 : `VideoClip.displayAspectRatio()` est désormais commun au preview ET au compositeur, y compris rotation de source 90/270°, au lieu d'avoir deux conventions de source. La rotation utilisateur de keyframe (p. ex. 2°) demeure indépendante.


## AUDIT sans code — retour terrain EASYCUT v0.0.5, 23/09/2026 — EASYCUT-PREVIEW-012 / EASYCUT-EXPORT-SMALL-013
### État attesté / limite
- Fab rapporte que le MP4 issu de la dernière version n'a PLUS le défaut de zoom d'export sur son scénario ; valoriser ce succès comme un retour utilisateur, pas comme preuve universelle de tous les formats. RÉFÉRENCE À PRÉSERVER : source portrait → projet carré/multipiste, zoom volontaire 321 % (ou 380 % choisi plus tard), positions/pivots/keyframes, synchronisation et ordre des plans, export final conforme. Ne jamais rétablir l'ancien rendu pour simplement rendre le preview rapide ; ne jamais supprimer le zoom utilisateur.
- Nouveau défaut terrain : l'aperçu v0.0.5 n'évolue plus en temps réel / peine à calculer les frames et le cadrage en lecture ou en manipulation. Aucun log performance, type de média précis ni FPS réellement décodé transmis avec ce signalement : distinguer image figée, lecture saccadée, retard au geste, horloge/règle décalée et perte A/V avant attribution de la cause.
### Contrat de performance à implémenter ensuite
- Playback/preview et horloge projet ne doivent pas dépendre d'une recomposition Compose chaque 80 ms pour rafraîchir des keyframes. Conserver une source temporelle monotone cohérente avec le lecteur actif, limiter les invalidations aux changements de matrice utiles et éviter seek/reprepare/rebind/surface churn hors besoin. L'audio doit conserver le temps projet durant les trous vidéo, transitions et changement de clips.
- Prévisualisation normale fluide à cadence de l'appareil quand décodable ; adaptation explicite (preview 720p/30, 480p/30 sur appareil limité) sans modifier résolution/cadence de sortie ni dégrader l'exactitude du cadrage. Proposer un état « Aperçu simplifié / rendu fidèle » sans prétendre qu'une piste unique démontre l'export multicouche. Pas de nouvelle option économique avant stabilisation instrumentée du moteur.
### Contrat de fonctionnalités demandé, non implémenté
- Dans la fenêtre d'export, conserver MP4, H.264/H.265 et réglages habituels ; bouton + Autres sur la ligne « Images/seconde » ouvrant une fenêtre lisible proposant « Source », présélections 1–5 i/s (animation/timelapse visuel), 10–12 i/s, 15, 20, 24, 25, 30, 50, 60, et un réglage entier libre de 1 à 60. Ne jamais modifier la vitesse/longueur du montage ou le son par seul changement de cadence ; vrai timelapse accéléré reste réglage de vitesse indépendant. L'API Media3 setFrameRate sur vidéo est un PLAFOND et ne garantit pas une cadence fixe exacte si la source est plus lente ; valider cadence réelle et politique de duplication des images après MP4.
- Résolutions supplémentaires 240p, 360p, 480p, 540p et anciennes 720p/1080p/1440p/4K, format et ratio inchangés ; « short side » selon ratio final et dimensions compatibles encodeur. Mode Économie d'espace indépendant des projets : presets petit/équilibré/haute qualité et personnalisation du débit, H.264 compatible / H.265 seulement si profil taille×FPS supporté ; AV1 ultérieurement sous condition d'encodage réel. Débits faibles à examiner (200 kbit/s à 2 Mbit/s pour bas profils), sans promettre qualité ou taille garantie. Réglages AAC : désactiver audio volontairement, voix mono 32/64, stéréo 96/128/192 kbit/s seulement si pipeline mixer/encodeur les supporte. Éviter de changer silencieusement la stéréo en mono.
- Estimer la taille à partir de la durée en secondes et des débits audio/vidéo, ajouter marge conteneur, annoter estimation et surface/temporaire libre. Mode « taille cible » n'est pas une garantie en VBR ni un contrôle qualité constant ; vérifier le résultat réel (`ExportResult` taille/débits, propriétés MP4) et signaler tout repli codec/cadence/résolution. Prévenir qu'une source 1 Go peut exiger temporairement de gros proxies même si sortie 480p réduite.
- Accessibilité exigée : libellés explicites, contrôles assez grands pour yeux/doigts, texte contrasté ajustable ; « + Autres » ne doit pas créer de minuscules choix illisibles. Séparer absolument qualité d'EXPORT, qualité d'APERÇU et compression des FICHIERS SOURCES. Plan uniquement, non codé dans cette intervention.


## EASYCUT v0.0.6 — EASYCUT-PREVIEW-012 (23/09/2026), intervention code
- Contrat : zoom/dézoom et déplacement deux doigts visibles pendant le geste, sur la dernière image déjà décodée même lorsque le codec ralentit ; garder le seuil rotation de 10° et la convention exacte export FIT/zoom/pivot/cadrage. Ne pas réencoder un fichier 1 Go pendant le geste ; aucun proxy 360p/480p automatique disponible dans ExoPlayer pour une source unique à ce stade. Ne PAS prétendre fournir une vraie décodification réduite sans source/proxy adapté.
- `FabVidVideoTextureView.applyPreviewTransform` mémorise ses paramètres source/canevas/ClipTransform/taille : sauter `TextureView.setTransform` si rien n'a changé, réappliquer après recréation/redimensionnement de surface. Exposer compteurs locaux de frames réellement présentées, matrices et branchements pour audit utilisateur facultatif.
- Le geste Compose injecte immédiatement `PreviewGestureMath.panZoom` dans la TextureView conservée, AVANT synchronisation du modèle de projet (enregistré une fois à fin de geste) ; la matrice affichée n'attend donc plus la recomposition de la timeline. Toute la couche tactile demeure non zoomée.
- En lecture simplifiée, recalculer directement la matrice d'une animation (keyframes/transitions) toutes ~33 ms sur la TextureView sans redessiner les contrôles à chaque étape ; conserver un échantillon `currentPositionMs` Compose ~80 ms. Le décodeur vidéo conserve sa cadence native, les positions source/projet et pistes continuent de changer correctement aux frontières. Le compteur à 30 Hz concerne les transformations, PAS la promesse d'un flux décodé à 30 ips.
- Ne re-préparer/synchroniser les lecteurs après changement de données QUE si les sources/trim/vitesse, ordre et temps des pistes, audio/résolution de durée ou opacité modifient l'identité média de `PreviewMediaIdentity`. Les zooms 321/380%, keyframes et luminosité n'ont pas d'effet sur cette identité ; le cadrage et l'export reçoivent pourtant immédiatement les valeurs stockées.
- Diagnostics `PERF preview` bornés et locaux toutes les ~10s (mode, frames présentées, matrices appliquées, nombre de surface binds, temps projet), sans réseau et sans modifier le dernier stage de crash. Ne pas confondre résultat CI avec validation Android, latence tactile, FPS GPU ni correction de tous les formats.

- En passant d'un aperçu CompositionPlayer au premier pinch, basculer vers `fallbackPlayer` sur la même surface AVANT de transformer la texture, pour ne pas appliquer pendant un instant le zoom deux fois à toute la composition. Ne réencoder ni ne créer de proxy pendant ce changement.

## EASYCUT v0.0.7 — EASYCUT-RECOVERY-013 (23/09/2026) — lot unique, validation Android requise
- Sur certaines vidéos seulement, le premier pinch CompositionPlayer→ExoPlayer peut entraîner seek/prepare et surface temporairement dépourvue de frame. Avant transfert, capturer UNE frame de TextureView, bornée à 480p côté low-RAM / 720p sinon, puis la transformer au doigt via matrice relative. Effacer seulement après onRenderedFirstFrame du lecteur de secours. En l'absence de snapshot valide, rechercher indépendamment une frame source synchronisée légère 360→480p low-RAM ou 480→720p autrement. Aucun getBitmap par mouvement, pas de nouvelle vidéo proxy ni de recompression en arrière-plan.
- Import H.264/H.265 : MP4 prioritaire sans réencodage, MPEG-TS et MKV toujours solutions de repli selon acceptation du muxer ; MPEG1/2 conserve TS en premier. Les projets historiques et les fichiers originaux ne sont pas remuxés rétroactivement ; source déjà extraite en TS reste TS tant qu'elle n'est pas réimportée.
- FFprobe : inventaire court vers fichier privé conservé ; si code natif échoue, fichier vide ou JSON invalide, seconde commande courte vers stdout borné <=4 Mio ; jamais ce repli pour l'inventaire image par image. Verrou commun entre FFprobe, remux d'import et miniatures FFmpegKit ; exports Media3 et lecteur vidéo non verrouillés. Supprimer le clearSessions import/miniatures qui pouvait effacer l'état des sessions partagées.
- Conservation absolue du MP4 d'export, ratios, gestes rotation seuil 10°, fichiers projet et cadence/résolution export. Nouveaux tests JVM de politiques d'aperçu/choix conteneur/arguments FFprobe ; seul un test Android réel sur vidéo problématique pourra valider qualité visuelle, position et indépendance du décodeur. Voir topo.md et todo.md.

## v0.0.8 — EASYCUT-RECOVERY-014 (23/09/2026), correctif après journal réel Fab
- Journal v0.0.7 : FFPROBE_KEYFRAME code=1 bytes=0 native "-o" already specified with caractères corrompus, scan facultatif échoué, puis préparation vidéo/audio et OPEN_PROJECT clips=1 ; aucun crash Android déclaré. L'inventaire court est distinct et fonctionne ; son repli stdout v0.0.7 ne réparait pas le scanner de frames.
- Désactiver l'entrée dans ce scanner FFprobe natif pour TOUTE taille tant que le writer Android -o n'est pas validé : employer immédiatement la résolution de l'inventaire, signaler SCAN_SKIPPED_NATIVE_UNVERIFIED comme stage non fatal, ne PAS journaliser de fausse erreur import. Une résolution qui change au cours d'une seule vidéo ne sera pas détectée dans ce mode ; ne pas prétendre résoudre ce cas. Aucun réencodage/source altérée.
- ExoPlayer déjà en aperçu simple : avant premier vrai rendu SUR LA SURFACE COURANTE seulement, autoriser récupération image-source 360/480 low-RAM ou 480/720 normale sans reprepare/seek/rebind ; si frame déjà présentée, continuer la matrice TextureView directe (pause du décodeur lors du geste attendue). Pont multipiste→simple v0.0.7 conservé.
- Journaux locaux PERF distinguent frames réellement présentées, matrices, redessins snapshot, images récupérées, état de lecture, délai depuis dernière frame et attente du pont. Des frames faibles alors que playing=false NE suffisent PAS à prouver une panne décodeur. Ne jamais journaliser URI privée.
- Préserver rotation volontaire 10°, UI touch coordinate system, keyframes, projet, audio et export MP4 existants. Tests JVM/CI ne valident pas GPU ni flux source Fab ; bilan dans todo.md, topo.md.

## EASYCUT v0.0.9 — EASYCUT-PREVIEW-015 (23/09/2026) — restauration ciblée du zoom mono-vidéo
- Contrat utilisateur P0 : retrouver une image visible qui suit le pinch/pan sans bascule de décodeur au premier geste sur un clip VIDÉO simple. Une CI verte ne prouve pas la fluidité Samsung : attendre la validation Fab avant de fermer le bug.
- Routage : un seul clip VisualMediaKind.VIDEO, aucune couche texte, transition, filtre ni correction de luminosité, y compris les keyframes de luminosité : ExoPlayer dès l'entrée projet ; une image fixe, texte/filtre/luminosité ou plusieurs clips gardent CompositionPlayer sauf ancienne règle unequalVideoSpans qui choisit l'aperçu compatible. Trim, vitesse, zoom, keyframes de transformation et pistes audio compatibles avec la voie simple existante.
- Après ajout d'un deuxième clip ou d'un effet nécessitant composition, sortie du routage automatique mono-vidéo vers CompositionPlayer quand pas de spans inégaux ; ne pas retirer un mode simple adopté antérieurement pour une autre raison à chaque recomposition. Le lecteur simple déjà actif au premier pinch ne subit ni bind, ni prepare ni seek du seul fait du geste ; on transforme directement sa TextureView existante. Pont frame v0.0.8 utilisable seulement si aucune frame encore disponible ou pour la voie multipiste historique.
- Ne pas remanier l'export ni ajouter des proxies. Scan FFprobe natif des variations de résolution toujours neutralisé v0.0.8 faute de writer validé. Version 0.0.9/code 42, tests de routage et cinq mémoires FAB Copilot même commit. Source/exports inchangés.

## v0.0.10 — EASYCUT-PREVIEW-016 — contrat prévisualisation Fab (23/09/2026)
- Clarification Fab : v0.0.3 affichait CORRECTEMENT la prévisualisation ; le DOUBLE ZOOM concernait L'EXPORT UNIQUEMENT. v0.0.9 import/OPEN_PROJECT OK, mode=simple state=3 mais aperçu incorrect. Ce point annule la précédente hypothèse « double zoom sur le preview 0.0.3 ».
- Rétablir le contrat de rendu preview de 0.0.3 : le seul propriétaire de scale, rotation, translation et pivot de l'image en mode simple est le graphicsLayer Compose de l'AndroidView. TextureView natif en matrice identité pour tous les modes ; CompositionPlayer fournit déjà son rendu propre. PreviewV03RenderPolicy rend exclusives les deux propriétés. Ne pas changer CompositionFactory, pipeline export Media3, structure de projet, trim/audio ni FFprobe.
- Retirer du geste les ponts de capture/récupération de frames de v0.0.7-0.0.8 et l'application native immédiate de transformations de v0.0.6 ; conserver v0.0.9 : un seul clip VIDEO simple démarre directement ExoPlayer sans bascule au premier pincement. Pour multipiste, la bascule historique reste autorisée au premier geste et le rendu est assuré par Compose.
- Tests JVM du contrat propriétaire unique ; CI et APK v0.0.10/code43. La restitution visuelle et le MP4 sans double zoom exigent deux observations réelles Fab, ne pas annoncer « corrigé sur Samsung » avant validation.

## Contrat confirmé par Fab — journal GET ERR et prévisualisation validée (23/09/2026)
- EASYCUT 0.0.10 : **prévisualisation validée « nickel » par Fab** ; premier export avant déplacement vertical vidéo 2 validé. Ne modifier ni réouvrir ces acquis pour traiter le bug d'export multipiste après déplacement (voir `debughistorical.md` : EXPORT-MOVE-017).
- Journal de diagnostic souhaité : véritable erreur/échec de l'action en rouge, information/étape réussie en vert, événement non fatal (étape facultative ignorée, délai dépassé avec récupération sans perte de fonctionnalité) en gris. Timeout fatal = rouge, pas gris. Toujours un libellé lisible en plus de la couleur, contraste et taille adaptés à la vision de Fab.
- Même journal « GET ERR » accessible depuis accueil **et depuis l'éditeur sans quitter le montage** ; commande « Effacer le journal » avec confirmation, sans supprimer projets/médias, avec conservation de l'identification de version et d'une éventuelle panne en cours ; remise à zéro clairement annoncée, pas « aucun problème n'a jamais eu lieu ».
- Export économique `EASYCUT-EXPORT-SMALL-013` demeure une fonctionnalité planifiée, NON CODÉE : bouton + Autres cadences et résolutions faibles/profils de débit/audio/taille estimée. Les comptes rendus de tests et incidents historiques n'ont pas vocation à faire disparaître ces petits chantiers.

## FAB-MISSION-001 — demandes immuables de Fab dans un fichier séparé (23/09/2026)
Décision explicite de Fab : **`ordres-de-mission.md`** est le registre des ORDRES DE MISSION de Fab, distinct de `todo.md` réservé aux tâches techniques et propositions de l'assistant. `topo.md` est un aide-mémoire utile à l'agent, facultatif pour Fab ; il n'est ni une faute de saisie à effacer ni un registre de mission. Aucun bug ne peut remplacer, requalifier, faire disparaître ou reprioriser une mission utilisateur ; seul Fab peut changer ses demandes. Une mission persiste (« pour plus tard », « en todo » et « code pas » compris) tant que son produit n'est pas réellement livré ; la validation de l'expérience Android exige le retour de Fab.
Missions ouvertes attestées : `FAB-EXPORT-FPS-001` (« + Autres » sur la cadence 1–60 i/s), `FAB-EXPORT-RES-002` (240/360/480/540p), `FAB-EXPORT-SIZE-003` (économie d'espace, AAC et taille estimée), `FAB-JOURNAL-COLOR-004` (rouge/vert/gris + libellés), `FAB-JOURNAL-ACCESS-005` (GET ERR aussi depuis l'éditeur), `FAB-JOURNAL-CLEAR-006` (bouton Vider avec confirmation). Le contrat détaillé demeure ici et le statut dans le registre. Prévisualisation 0.0.10 et premier export avant déplacement validés ; bug MP4 après changement de piste dans `debughistorical.md`.
